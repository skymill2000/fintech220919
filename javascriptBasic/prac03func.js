function plus(p1, p2) {
  return p1 + p2;
}

console.log(plus(1, 3));

const plus2 = (p1, p2) => {
  return p1 + p2;
};

console.log(plus2(1, 3));

// #work4 arrow function 으로 minus(), div(), multi() 빼기 나누기 곱하기 기능 구현하기;
